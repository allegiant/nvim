# 整理未跟踪文件忽略规则

## Goal

Analyze the current untracked files after Trellis initialization and update the narrowest appropriate ignore rules so files that should remain local/runtime-only do not keep appearing in `git status`, while preserving project files that future AI sessions need.

## What I already know

- User asked to analyze untracked files and add unnecessary-to-track files to `.gitignore`.
- Root `.gitignore` currently only ignores `plugin` and `lazy-lock.json`.
- Current untracked paths are generated by Trellis/Claude initialization: `.claude/`, `.trellis/` subtrees, and `AGENTS.md`.
- `.trellis/.gitignore` already ignores Trellis runtime-local files such as `.developer`, `.current-task`, `.runtime/`, `.agents/`, `.session-id`, temp files, and Python caches.
- Trellis generated-files documentation says project-editable files include `.trellis/workflow.md`, `.trellis/config.yaml`, `.trellis/spec/**`, `.trellis/tasks/**`, `.trellis/workspace/**`, `.trellis/scripts/**`, and platform files like `.claude/**`.
- Trellis workspace documentation says `.trellis/workspace/` stores cross-session memory and can be committed by default via session auto-commit.

## Assumptions (temporary)

- Keep Trellis/Claude project infrastructure trackable unless it is clearly local runtime state.
- Do not ignore files that are needed for future sessions, sub-agent context injection, task workflow, or project conventions.
- This task should not delete files; it should only update ignore rules after classification.

## Open Questions

- None.

## Requirements (evolving)

- Inspect current untracked files and classify them as trackable project files versus local/runtime-only files.
- Ignore `.trellis/workspace/**` for this personal Neovim config so session journals/indexes stay local-only.
- Update ignore rules only for files/directories that should not be tracked.
- Prefer the narrowest ignore location: Trellis-internal local state should go in `.trellis/.gitignore`; repository-wide editor/runtime artifacts should go in root `.gitignore`.
- Avoid ignoring core Trellis/Claude project files required for workflow, hooks, skills, specs, or active tasks.
- Keep existing ignore entries unless they are clearly wrong.

## Classification

- Trackable project configuration/context: `.claude/**`, `.trellis/workflow.md`, `.trellis/config.yaml`, `.trellis/scripts/**`, `.trellis/spec/**`, `.trellis/tasks/**`, `.trellis/.version`, `.trellis/.template-hashes.json`, and `AGENTS.md`; these support future AI sessions, workflow hooks, skills, specs, and active task context.
- Local/runtime-only Trellis state: `.trellis/workspace/**`; in this personal Neovim config it contains session journals/indexes and should remain local-only.
- Existing local/runtime-only Trellis state already covered by `.trellis/.gitignore`: `.trellis/.developer`, `.trellis/.current-task`, `.trellis/.runtime/**`, `.trellis/.agents/**`, temp/backup files, and Python caches.

## Acceptance Criteria (evolving)

- [x] Current untracked paths are classified with rationale.
- [x] `.trellis/workspace/**` no longer appears as untracked noise.
- [x] Ignore rules include only local/runtime-only patterns that should not be tracked.
- [x] Ignore rules do not hide Trellis specs, workflow scripts, hooks, skills, task docs, or `AGENTS.md` if they are intended project configuration.
- [x] `git status --short` after the change shows fewer noisy untracked files or the remaining files are intentionally trackable.

## Definition of Done

- Root `.gitignore` updated if needed.
- No generated caches, runtime state, or local-only identity/session files are accidentally tracked.
- No broad ignore rule hides important Trellis project files.
- `git status --short` reviewed after the update.

## Out of Scope

- Committing all remaining Trellis initialization files.
- Deleting generated files.
- Changing Trellis workflow behavior or session auto-commit settings unless required for ignore behavior.

## Technical Notes

- Existing root `.gitignore`: `.gitignore`.
- Existing nested Trellis ignore: `.trellis/.gitignore`.
- Relevant generated-file reference: `.claude/skills/trellis-meta/references/local-architecture/generated-files.md`.
- Relevant workspace reference: `.claude/skills/trellis-meta/references/local-architecture/workspace-memory.md`.
- Current likely trackable project files: `.claude/**`, `.trellis/workflow.md`, `.trellis/config.yaml`, `.trellis/scripts/**`, `.trellis/spec/**`, `.trellis/tasks/**`, `.trellis/.version`, `.trellis/.template-hashes.json`, `AGENTS.md`.
- Current likely local/runtime-only files are already covered by `.trellis/.gitignore`: `.trellis/.developer`, `.trellis/.current-task`, `.trellis/.runtime/**`, Python caches, temp files.
